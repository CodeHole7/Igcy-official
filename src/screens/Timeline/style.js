import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    timelineButton: {
        borderColor: '#000',
        borderWidth: 1,
        borderRadius: 12,
        paddingHorizontal: 60,
        paddingVertical: 8,
    },
    timelineButtonText: {
        color: '#000',
        fontSize: 18,
        fontWeight: '500',
    }
});